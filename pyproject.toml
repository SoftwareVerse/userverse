[project]
name = "vestabooks"
version = "0.0.2"
description = "Vestabooks is an open-source platform designed to make managing users, organizations, and their relationships simple and efficient."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [